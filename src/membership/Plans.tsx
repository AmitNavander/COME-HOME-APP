import type { ReactNode } from 'react';
export const FREE_FEATURES = ['Current meditation, breathing, quiet timer and sleep collection', 'Seven-day Manifestation Foundations', 'Personal journal, saved practices and foundation progress'];
export const PLUS_FEATURES = ['Full 21-day Manifesting Through Water™ workshop', 'Structured goal plan: preparation, visualization, actions and reviews', 'Vision board with images, intentions and phone wallpaper export'];
export default function Plans() {
  return <section className="journey-card" aria-label="Free and paid plans"><div className="eyebrow">Choose your level</div><h2 className="serif">Start free. Go deeper with COME HOME+.</h2><div className="plans-grid"><section><h3>Free</h3><p>No payment required</p><ul>{FREE_FEATURES.map(text => <li key={text}>{text}</li>)}</ul></section><section><h3>COME HOME+ · Paid</h3><p>Membership pricing will be announced before launch</p><ul>{PLUS_FEATURES.map(text => <li key={text}>{text}</li>)}</ul></section></div><p className="journey-muted">Early access: COME HOME+ journeys are currently open without charge. No subscription starts and no payment is collected. Account saving is optional and controlled from You → Privacy and account.</p></section>;
}
export function PremiumPreview({ children }: { children: ReactNode }) { return <section className="premium-preview"><p className="access-badge">COME HOME+ · Early access open</p>{children}</section>; }
