import type { ReactNode } from 'react';
export const FREE_FEATURES = ['Current meditation, breathing, quiet timer and sleep collection', 'Seven-day Manifestation Foundations', 'Personal journal, saved practices and foundation progress'];
export const PLUS_FEATURES = ['Full 21-day Manifesting Through Water™ workshop', 'Structured goal plan: preparation, visualization, actions and reviews', 'Vision board with images, intentions and phone wallpaper export'];
export default function Plans() {
  return <section className="journey-card" aria-label="Free and paid plans"><div className="eyebrow">Choose your level</div><h2 className="serif">Start free. Go deeper with COME HOME+.</h2><div className="plans-grid"><section><h3>Free</h3><p>No payment required</p><ul>{FREE_FEATURES.map(text => <li key={text}>{text}</li>)}</ul></section><section><h3>COME HOME+ · Paid</h3><p>Paid launch coming soon</p><ul>{PLUS_FEATURES.map(text => <li key={text}>{text}</li>)}</ul></section></div><p className="journey-muted">COME HOME+ features are open as a free development preview for now. Pricing and billing are not live. Nothing here starts a subscription or charges you. Purchases will be enabled only when secure payment and account access are connected.</p><p className="journey-muted">Your current journal, plans and vision board save in this browser. Cloud sync is not included yet.</p></section>;
}
export function PremiumPreview({ children }: { children: ReactNode }) { return <section className="premium-preview"><p className="access-badge">COME HOME+ · Paid at launch · Free preview now</p>{children}</section>; }
